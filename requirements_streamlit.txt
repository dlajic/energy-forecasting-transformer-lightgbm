# =============================
# Requirements for Energy Forecasting Streamlit App (Deployment)
# =============================

# Python 3.11 compatible
# Streamlit Cloud runs on CPU, so we use CPU-compatible packages only

# Moment Foundation Model (used for inference)
momentfm @ git+https://github.com/moment-timeseries-foundation-model/moment.git@37a8bde4eb3dd340bebc9b54a3b893bcba62cd4f

# === Core Python stack ===
numpy==1.25.2
pandas==2.2.2
matplotlib==3.10.0

# === ML for Inference Only ===
scikit-learn==1.6.1
torch==2.1.0        # CPU-compatible
# torchvision / torchaudio not needed for inference in this app

# === LightGBM ===
lightgbm==4.3.0

# === Streamlit App ===
streamlit>=1.30.0
plotly>=5.0.0
